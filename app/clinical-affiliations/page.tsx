import { hospitals } from "@/lib/data";

export const metadata = { title: "Clinical Affiliations | Imam Institute", description: "Explore clinical training and house job affiliations for Imam Institute students." };

export default function ClinicalAffiliationsPage() {
  return <section className="bg-white"><div className="mx-auto max-w-7xl px-6 py-24 md:px-10"><p className="text-sm font-semibold uppercase tracking-[0.25em] text-crimson">Clinical practice</p><h1 className="mt-4 text-5xl font-semibold tracking-[-0.03em] text-navy text-display">Clinical Affiliations</h1><p className="mt-4 max-w-3xl text-base leading-8 text-ink-muted">Our students learn in real healthcare environments with exposure to diverse departments, patients, and professional teams.</p><div className="mt-12 grid gap-6 md:grid-cols-2">{hospitals.map((hospital) => <article key={hospital.name} className="rounded-[1.75rem] border border-border bg-off-white p-8"><div className="flex items-start justify-between gap-4"><span className="text-4xl font-semibold text-crimson/40">{hospital.number}</span>{hospital.badge && <span className="rounded-full bg-gold-pale px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] text-ink">{hospital.badge}</span>}</div><h2 className="mt-6 text-2xl font-semibold text-navy">{hospital.name}</h2>{hospital.fullName && <p className="mt-1 text-sm font-medium text-ink-muted">{hospital.fullName}</p>}<p className="mt-4 text-sm font-semibold text-crimson">{hospital.location}</p><p className="mt-4 text-sm leading-7 text-ink-muted">{hospital.desc}</p></article>)}</div></div></section>;
}
